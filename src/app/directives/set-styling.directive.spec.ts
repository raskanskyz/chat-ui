import { SetStylingDirective } from './set-styling.directive';

describe('SetStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new SetStylingDirective();
    expect(directive).toBeTruthy();
  });
});
